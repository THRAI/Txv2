# Handoffs

Store dated resume packets here as JSON: `YYYY-MM-DD-short-title.json`.

Use `.agents/commands/create-handoff.md` or create records directly with
`cargo xtask progress new handoff`.

Useful listing command:

```sh
cargo xtask progress list handoffs
cargo xtask progress list handoffs --json
```
