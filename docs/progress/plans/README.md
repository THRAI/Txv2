# Plans

Store dated implementation, cleanup, and audit plans here as JSON:
`YYYY-MM-DD-short-title.json`.

Use `.agents/commands/create-plan.md` or create records directly with
`cargo xtask progress new plan`.

Useful listing command:

```sh
cargo xtask progress list plans
cargo xtask progress list plans --json
```
