[package]
name = "tx-hal"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
path = "src/lib.rs"
test = false
doctest = false

[features]
default = []
# LA64 keeps LSX/LASX state in the architecture-neutral user context.  Other
# targets must not pay for that 1 KiB payload on every trap round-trip.
extended-user-vector-context = []
